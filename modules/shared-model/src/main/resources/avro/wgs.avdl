@namespace("de.dfki.cos.basys.processcontrol.model")
protocol WGSMessages {
	enum NotificationType {
		LEADING_INTO_WRONG_DIRECTION, WRONG_DIRECTION_REACHED, GRASPED_AT_WRONG_LOCATION, WRONG_QUANTITY_TAKEN
	}

	record Notification {
		NotificationType type;
		boolean show;
	}

	record StepChange {
		string workstepId;
	}
}